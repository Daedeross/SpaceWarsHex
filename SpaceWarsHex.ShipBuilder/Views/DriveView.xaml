<rxui:ReactiveUserControl x:Class="SpaceWarsHex.ShipBuilder.Views.DriveView"
                          x:TypeArguments="vm:DriveViewModel"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:local="clr-namespace:SpaceWarsHex.ShipBuilder.Views"
                          xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
                          xmlns:rxui="http://reactiveui.net"
                          xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                          xmlns:vm="clr-namespace:SpaceWarsHex.ShipBuilder.ViewModels;assembly=SpaceWarsHex.ShipBuilder.ViewModels"
                          mc:Ignorable="d" 
                          d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.Resources>
            <Style TargetType="mah:NumericUpDown">
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="1" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0">Max Warp</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="0" Width="100" 
                           Name="WarpSpin"
                           Minimum="0" Maximum="1000" />

        <Label Grid.Column="0" Grid.Row="1">Acceleration</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="1" Width="100" 
                           Name="AccelerationSpin"
                           Minimum="0" Maximum="1000" />

    </Grid>
</rxui:ReactiveUserControl>
