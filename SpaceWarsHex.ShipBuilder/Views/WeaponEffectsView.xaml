<rxui:ReactiveUserControl x:Class="SpaceWarsHex.ShipBuilder.Views.WeaponEffectsView"
                          x:TypeArguments="vm:WeaponEffectsViewModel"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:local="clr-namespace:SpaceWarsHex.ShipBuilder.Views"
                          xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
                          xmlns:rxui="http://reactiveui.net"
                          xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                          xmlns:model="clr-namespace:SpaceWarsHex.Model;assembly=SpaceWarsHex.Contract"
                          xmlns:vm="clr-namespace:SpaceWarsHex.ShipBuilder.ViewModels;assembly=SpaceWarsHex.ShipBuilder.ViewModels"
                          xmlns:sys="clr-namespace:System;assembly=mscorlib"
                          mc:Ignorable="d">
    <UserControl.Resources>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="DamageTypeValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="model:DamageKind" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="UpBtn" Grid.Column="0" Grid.Row="0">Up</Button>
        <Button x:Name="DownBtn" Grid.Column="1" Grid.Row="0">Down</Button>
        <Button x:Name="AddBtn" Grid.Column="2" Grid.Row="0">Add</Button>
        <Button x:Name="RemoveBtn" Grid.Column="3" Grid.Row="0">Remove</Button>
        
        <DataGrid Grid.Row="1" Grid.ColumnSpan="4" Name="EffectsGrid" SelectionMode="Single"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Type"
                                        MinWidth="100"
                                        ItemsSource="{Binding Source={StaticResource DamageTypeValues}}"
                                        SelectedItemBinding="{Binding DamageKind}" />
                <mah:DataGridNumericUpDownColumn Header="Value"
                                                 Interval="1" Minimum="0"
                                                 
                                                 Binding="{Binding DamageValue, Converter={StaticResource IntegerToDoubleConverter}}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</rxui:ReactiveUserControl>
