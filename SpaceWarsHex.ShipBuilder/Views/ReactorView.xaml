<rxui:ReactiveUserControl x:Class="SpaceWarsHex.ShipBuilder.Views.ReactorView"
                          x:TypeArguments="vm:ReactorViewModel"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:local="clr-namespace:SpaceWarsHex.ShipBuilder.Views"
                          xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
                          xmlns:rxui="http://reactiveui.net"
                          xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                          xmlns:vm="clr-namespace:SpaceWarsHex.ShipBuilder.ViewModels;assembly=SpaceWarsHex.ShipBuilder.ViewModels"
                          mc:Ignorable="d" 
                          d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.Resources>
            <Style TargetType="mah:NumericUpDown">
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="1" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0">Cruise Power</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="0" Width="100" 
                           Name="CruiseSpin"
                           Minimum="0" Maximum="1000" />

        <Label Grid.Column="0" Grid.Row="1">Attack Power</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="1" Width="100" 
                           Name="AttackSpin"
                           Minimum="0" Maximum="1000" />

        <Label Grid.Column="0" Grid.Row="2">Emergency Power</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="2" Width="100" 
                           Name="EmergencySpin"
                           StringFormat="+0;-#"
                           d:Value="5"
                           Minimum="0" Maximum="1000" />

        <Label Grid.Column="0" Grid.Row="3">Turns at Attack</Label>
        <mah:NumericUpDown Grid.Column="1" Grid.Row="3" Width="100" 
                           Name="TurnsSpin"
                           Minimum="0" Maximum="100" />

        <local:ThresholdsView Grid.Column="2"
                              Grid.RowSpan="5"
                              x:Name="ThresholdsView"
                              />
    </Grid>
</rxui:ReactiveUserControl>
